import React from 'react';
import Logo from '../Logo/Logo'
import './Toolbar.css'

const toolbar = (props) => (
    <header className='Toolbar'>
        <div>MENU</div>
        {/* <div className='Logo'>
            <Logo />
        </div> */}
        <nav>
            ...
        </nav>
    </header>
)

export default toolbar;